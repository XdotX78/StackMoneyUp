# Test Environment Configuration
# Copy this to .env.test.local and fill in your test values

# ⚠️ IMPORTANT: Use a separate test/dev Supabase project
# DO NOT use production credentials here!

# Supabase Configuration (Test/Dev Project)
NEXT_PUBLIC_SUPABASE_URL=https://your-test-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-test-anon-key

# Test User Credentials
# Create this user in your test database before running tests
TEST_USER_EMAIL=test@example.com
TEST_USER_PASSWORD=TestPassword123!

# Optional: Site URL for E2E tests
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Note: Integration and E2E tests will create and clean up their own test data
# However, the TEST_USER_EMAIL must exist in your test database with appropriate permissions

